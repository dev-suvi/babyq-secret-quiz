import './globals.css';

export const metadata = { title: 'BabyQ • Top Secret 🤫', description: 'A little BabyQ mystery. Your secret is waiting. 👀' };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
